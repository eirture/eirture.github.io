sudo: false
language: node_js
node_js:
  - 10
cache: npm
branches:
  only:
    - src
script:
  - make deploy # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  target-branch: master
  keep-history: true
  on:
    branch: src
  local-dir: public
